% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/as_quanteda_corpus.R
\name{as_quanteda_corpus}
\alias{as_quanteda_corpus}
\title{Convert a krpoltext data.table to a quanteda corpus}
\usage{
as_quanteda_corpus(x, text_field = "text", docid_field = NULL, ...)
}
\arguments{
\item{x}{A \code{data.table} (or \code{data.frame}) with at least a \code{text} column.}

\item{text_field}{Character; name of the column containing document text.
Defaults to \code{"text"}.}

\item{docid_field}{Character or \code{NULL}; name of the column to use as
document IDs. If \code{NULL}, row numbers are used.}

\item{...}{Additional arguments passed to \code{\link[quanteda:corpus]{quanteda::corpus()}}.}
}
\value{
A \code{quanteda} corpus object.
}
\description{
Creates a \code{\link[quanteda:corpus]{quanteda::corpus()}} object from a \code{data.table} loaded by
\code{\link[=load_campaign_booklet]{load_campaign_booklet()}} or \code{\link[=load_party_statements]{load_party_statements()}}. The \code{text} column
is used as the document text; all other columns become document-level
variables (docvars).
}
\examples{
if (requireNamespace("quanteda", quietly = TRUE)) {
  dt <- data.table::data.table(
    id = c("doc-1", "doc-2"),
    text = c("first text", "second text"),
    year = c(2020L, 2021L)
  )

  corp <- as_quanteda_corpus(dt, docid_field = "id")
  corp
}
}
